# Backend Configuration
# Port for the backend server (default: 8000)
PORT=8000

# Firestore Configuration
# For local development with Firestore emulator, set this to localhost:8080
# For production, leave commented and set GOOGLE_APPLICATION_CREDENTIALS
# FIRESTORE_EMULATOR_HOST=localhost:8080

# Google Cloud credentials (for production)
# Path to your Firebase service account JSON file
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/serviceAccountKey.json

# Frontend Configuration (in frontend/.env.local)
# NEXT_PUBLIC_API_URL=http://localhost:8000
